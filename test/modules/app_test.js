
var App = require('modules/app');

describe('App', function() {
  beforeEach(function() {
  });

  it('exposes a init method', function() {
    expect(1).to.equal(1);
  });

  afterEach(function() {
  });
});

