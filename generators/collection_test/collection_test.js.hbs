
var Collection = require('models/base/collection');
var {{#camelize}}{{name}}{{/camelize}} = require('models/{{name}}');
var {{#camelize}}{{pluralName}}{{/camelize}} = require('models/{{pluralName}}');

describe('{{#camelize}}{{name}}{{/camelize}}', function() {
  beforeEach(function() {
    this.model = new {{#camelize}}{{name}}{{/camelize}}();
    this.collection = new {{#camelize}}{{pluralName}}{{/camelize}}();
  });

  afterEach(function() {
    this.model = null;
    this.collection = null;
  });
});

