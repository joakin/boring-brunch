
var {{#camelize}}{{name}}{{/camelize}} = require('modules/{{name}}');

describe('{{#camelize}}{{name}}{{/camelize}}', function() {
  beforeEach(function() {
  });

  afterEach(function() {
  });
});

